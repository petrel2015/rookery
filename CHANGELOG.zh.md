# 更新日志

本项目所有值得注意的变更都记录在此文件中。
格式遵循 [Keep a Changelog](https://keepachangelog.com/en/1.1/)，
版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

> **关于版本历史：** 本仓库目前只有一个 commit（2026-08-27），无 Git
> tag、无 GitHub Release——已通过 `git tag`、`git ls-remote --tags` 与
> GitHub Releases 页面核实。因此不存在更细粒度的分版本历史；下面这条
> 条目汇总初始版本的完整能力集。原始历史见 `git log`。后续变更将随
> tag 的创建按版本逐条记录于此。

## [Unreleased]

### 变更

- 分类筛选改为按「能否在线试用」划分：全部 / 可在线试用 / 仅看介绍
  （按是否配置 `demo` 链接，当前为 12 / 9 / 3）。`category` 字段保留，
  仅用于卡片类型徽章与首页指标，不再参与筛选。

## [0.1.0] - 2026-08-27

首个公开版本。首次发布于 2026-08-27；本条目汇总至当前版本的完整功能集。

### 新增

- 由单一数据文件（`src/data/projects.js`）驱动的个人项目墙——预置
  12 个项目（2 扩展与应用、7 在线工具、3 游戏）；UI 不硬编码任何项目信息。
- 网格 / 列表双视图：上图下文的卡片墙（响应式：宽屏 3~4 列、平板
  2 列、手机 1 列）与左图右文的横向条目；选择持久化到
  `localStorage`（`pw-layout`）。
- 带实时数量的分类筛选：全部 / 扩展与应用 / 在线工具 / 游戏。
- 暗色模式：手动切换、默认跟随系统偏好、选择持久化（`pw-theme`）；
  首屏绘制前的内联主题恢复脚本避免闪白。
- 无截图兜底：没有截图的项目渲染「坐标纸 + 首字母」的生成式占位封面；
  `coverFit: 'contain'` 选项适配 App 图标类素材。
- 每张卡片的 GitHub 与在线体验链接；体验链接文案可按记录自定义
  （`demoLabel`）。
- 瑞士国际主义风格设计体系（纸白 / 墨黑 / 一抹红），含亮暗双主题。
- 开发者工具：`scripts/verify.mjs`（经 playwright-core 的 11 项布局与
  行为断言）与 `scripts/shot.mjs`（验证截图）。
- Vite 相对 base（`./`）构建，静态产物可托管到任意子路径。
